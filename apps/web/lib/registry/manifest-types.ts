// Moved to @usemount/shared (render-bearing, locked contract). This stub
// re-exports the names so existing @/lib/registry/manifest-types consumers
// stay unedited.
export type {
  ComponentManifest,
  ComponentManifestControls,
  AnyComponentManifest,
} from "@usemount/shared"
